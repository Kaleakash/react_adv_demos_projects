const initialState = {
    name :"Admin",
    customer:{cid:1,name:"Raj",age:21}
}
// Reducer
const reducer = (state = initialState, action) => {
        // as of now no action. 
      return state;
  
};

export default reducer;